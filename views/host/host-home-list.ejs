<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head") %>
  <body class="bg-gradient-to-br from-[#fff1f5] via-[#f3f4f6] to-[#e0e7ff]">
    <%- include("../partials/header") %>

    <main class="px-6 py-12 min-h-[80vh]">
      <h2
        class="text-3xl sm:text-4xl font-extrabold mb-12 text-center text-[#ff385c] tracking-wide"
      >
        Your Hosted Homes
      </h2>

      <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-10 place-items-center">
        <% registerdHomes.forEach((home) => { %>
        <li
          class="backdrop-blur-xl bg-white/70 border border-[#ff385c]/30 rounded-2xl 
          shadow-xl w-full max-w-[380px] overflow-hidden transform transition-all duration-300
          hover:scale-105 hover:shadow-2xl hover:-translate-y-1"
        >
          <img
            src="<%= home.image %>"
            alt="<%= home.houseName %>"
            class="w-full h-44 object-cover"
          />

          <div class="p-6 flex flex-col space-y-3">

            <h3 class="text-xl font-bold text-gray-900 truncate">
              <%= home.houseName %>
            </h3>

            <p class="text-[#ff385c] font-semibold">
              ₹<%= home.price %> <span class="text-gray-600 font-normal">/ night</span>
            </p>

            
            <p class="text-gray-700 font-medium flex items-center gap-2">
              <svg class="w-4 h-4 text-[#ff385c]" fill="currentColor" viewBox="0 0 20 20">
                <path d="M10 2a6 6 0 00-6 6c0 4.418 6 10 6 10s6-5.582 6-10a6 6 0 00-6-6zm0 8a2 2 0 110-4 2 2 0 010 4z"/>
              </svg>
              <span class="truncate"><%= home.location %></span>
            </p>

            <div class="flex justify-between items-center pt-4">

             
              <div class="flex gap-2">
                <a
                  href="/host/edit-home/<%= home._id %>?editing=true"
                  class="px-4 py-2 bg-blue-600 text-white text-sm font-bold rounded-md 
                  hover:bg-blue-700 active:scale-95 transition shadow"
                >
                  Edit
                </a>

                <form action="/host/delete-home/<%= home._id %>" method="POST">
                  <button
                    class="px-4 py-2 bg-[#ff385c] text-white text-sm font-bold rounded-md 
                    hover:bg-[#db2347] active:scale-95 transition shadow"
                    onclick="return confirm('Are you sure to delete? 🚫')"
                  >
                    Delete
                  </button>
                </form>
              </div>

              
              <p class="text-yellow-500 font-bold flex items-center gap-1">
                ⭐ <%= home.rating %>
              </p>

            </div>
          </div>
        </li>
        <% }); %>
      </ul>
    </main>
  </body>
</html>
