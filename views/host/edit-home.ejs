<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head") %>
  <body>
    <%- include("../partials/header") %>
    <main
      class="flex flex-col items-center justify-center min-h-[70vh] bg-gradient-to-br from-[#fff1f5] via-[#f3f4f6] to-[#e0e7ff] py-12 px-2"
    >
      <form
        action="/host/<%= editing ? "edit-home" : "add-home"%>"
        method="POST"
        class="bg-white p-8 rounded-2xl shadow-2xl flex flex-col gap-6 w-full max-w-md border border-[#ff385c]/20"
      >
        <h1
          class="text-3xl font-extrabold mb-2 text-[#ff385c] text-center tracking-tight drop-shadow"
        >
          <%= editing ? "Edit" : "Register " %> your Home on Airbnb
        </h1>
        <input type="hidden" name="id" value="<%= editing ? home.id : '' %>">
        <input
         <%= editing ? `value=${home.houseName}` : '' %>
          type="text"
          name="houseName"
          placeholder="Enter your House Name"
          class="border-2 border-[#ff385c] rounded-md px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#ff385c] text-lg bg-gray-50"
          required
        />
        <input
        <%= editing ? `value=${home.price}` : '' %>
          type="text"
          name="price"
          placeholder="Price per night"
          class="border-2 border-[#ff385c] rounded-md px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#ff385c] text-lg bg-gray-50"
          required
        />
        <input
        <%= editing ? `value=${home.location}` : '' %>
          type="text"
          name="location"
          placeholder="Enter your house location"
          class="border-2 border-[#ff385c] rounded-md px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#ff385c] text-lg bg-gray-50"
          required
        />
        <input
        <%= editing ? `value=${home.rating}` : '' %>
          type="text"
          name="rating"
          placeholder="Rating out of 5"
          class="border-2 border-[#ff385c] rounded-md px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#ff385c] text-lg bg-gray-50"
          required
        />
        <input
        <%= editing ? `value=${home.image}` : '' %>
          type="text"
          name="image"
          placeholder="Enter your image URL"
          class="border-2 border-[#ff385c] rounded-md px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#ff385c] text-lg bg-gray-50"
          required
        />
        <textarea
          name="description"
          placeholder="Description of your home"
          class="border-2 border-[#ff385c] rounded-md px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#ff385c] text-lg bg-gray-50"
        ><%= editing ? `${home.description}` : '' %></textarea>
        <input
        
          type="submit"
          value="<%= editing ? "Update Home" : "Add Home" %>"
          class="bg-[#ff385c] text-white font-bold py-3 rounded-md cursor-pointer transition-all duration-200 hover:opacity-90 hover:scale-105 shadow"
        />
      </form>
    </main>
  </body>
</html>
